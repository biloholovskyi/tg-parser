# Phase 03 — E2E

Статус: done

Требуемые правила: `.claude/rules/testing.md`, `.claude/rules/architecture.md`

## Цель

Приложение поднимается целиком и отвечает по HTTP предсказуемо, ни разу не обращаясь к Telegram.

## Заметки по реализации

Сценарии пишутся с нуля в `test/`. Приложение поднимается через тестовый модуль Nest с подменённым провайдером `TelegramService`.

## Объём

- Health-маршрут отвечает при отсутствующих переменных окружения Telegram.
- Полный цикл авторизации по шагам с подменённым сервисом.
- Выборка постов: успешный ответ и отказ при отсутствии сессии.
- Отклонение неверного тела запроса на уровне приложения.
- Ни один сценарий не создаёт настоящий клиент и не открывает соединение.

## Чеклист

- [x] Сценарии в `test/*.e2e-spec.ts`
- [x] `TelegramService` подменён провайдером
- [x] Проверено отсутствие сетевых обращений
- [x] Прогон стабилен при повторном запуске

## Команды проверки

- `rtk npm run test:e2e`

## Критерии приёмки

- Прогон E2E зелёный и занимает меньше минуты.
- Отсутствие переменных окружения Telegram не мешает старту приложения в тесте.

## Доказательства

- Сценарии уже существовали (41 тест); фаза добавила 6: полный цикл авторизации с веткой 2FA и неверным кодом, непустой ответ постов, новый `test/no-network.e2e-spec.ts`.
- `no-network.e2e-spec.ts` подменяет конструктор `TelegramClient` и доказывает, что он ни разу не вызван на health, auth, me и posts.
- `rtk npm run test:e2e`: 47 из 47, 4 набора; три прогона подряд — 3.9, 3.3 и 3.2 секунды.
- Health отвечает без переменных окружения Telegram (`app.e2e-spec.ts`).
- Не покрыто в E2E: 429 по лимиту на номер и 429 от Telegram на маршруте auth; лимит на номер покрыт юнит-спеком guard.
