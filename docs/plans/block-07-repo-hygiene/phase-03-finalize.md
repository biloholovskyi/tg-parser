# Phase 03 — Finalize

Статус: done

Требуемые правила: `.claude/rules/post-code-workflow.md`, `.claude/rules/drift-audit.md`, `.claude/rules/versioning-changelog.md`

## Цель

Репозиторий чист, документы правдивы, версия согласована.

## Объём (чеклист закрытия, один гейт)

- [x] Post-code: `rtk npm run lint && rtk npm run build && rtk npm test` — подтверждение, что чистка не задела сборку
- [x] Проверка дрейфа через агент `docs-drift-auditor`: `README.md` и `CLAUDE.md` соответствуют коду
- [x] Согласование версии, ветки и журнала подтверждено
- [x] Подготовка к коммиту без выполнения коммита

Тир `small`: отчёт о завершении и `reflect.md` не создаются.

## Команды проверки

- `rtk npm run lint`
- `rtk npm run build`
- `rtk npm test`

## Критерии приёмки

- Сборка и тесты зелёные после удаления файлов.
- Отчёт о дрейфе не содержит расхождений в документах корня.

## Итог

- Post-code: lint, build, 484 unit-теста — зелёные.
- Дрейф: отчёт `docs/reviews/drift-audit-2026-09-24.md`; четыре расхождения в документах исправлены.
- Вне границ блока, передано пользователю: дефект `postUrl` при `@` в имени канала (код), расхождение `DEPLOYMENT.md` с `runtime-resources.md` о файловой системе при перезапуске, относительные ссылки в `docs/integrations/` против правила ссылок.
